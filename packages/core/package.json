{
  "name": "@another-hour/core",
  "version": "1.0.0",
  "description": "Core time calculation logic for Another Hour",
  "main": "src/index.js",
  "browser": "dist/core.browser.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "build": "esbuild src/index.js --bundle --outfile=dist/core.browser.js --format=esm",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "another-hour",
    "time",
    "calculation"
  ],
  "author": "Another Hour Team",
  "license": "MIT",
  "devDependencies": {
    "@jest/globals": "^29.5.0",
    "esbuild": "^0.25.5",
    "jest": "^29.5.0"
  }
}